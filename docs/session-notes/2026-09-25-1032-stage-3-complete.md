# Stage 3 transactions completed

**Date:** 2026-09-25 10:32 CST

## Delivered

- Added Drift schema version 2 and the 1→2 migration for the singleton `transaction_draft_records` table while preserving version 1 categories and transactions.
- Added calendar-month and epoch-day utilities, transaction domain validation, joined monthly queries, transaction create/update/delete operations, and atomic create-plus-draft-clear behavior.
- Added shared in-memory month navigation for Transactions and Statistics, compact date-grouped transaction rows, signed CNY amounts, and category paths with localized visible strings.
- Added the reusable new/edit transaction bottom sheet with date, amount, type, category, optional subcategory, note, inline category creation, clear-draft, save, and confirmed delete flows.
- Persisted one new-transaction draft across form dismissals and app restarts, sanitizing category selections after category deletion or migration. Edit forms do not write drafts.
- Prevented concurrent form openings and blocked dismissal while save, delete, or clear-draft work is active, avoiding a race that could restore a stale draft after a successful create.
- Corrected the draft singleton constraint so it no longer recursively referenced its Drift getter; regenerated the database code, schema v2 snapshot, and migration helpers.

## Verification

- `.local-tooling/flutter/bin/dart format --output=none --set-exit-if-changed lib test`: passed for 34 files.
- `flutter analyze`: passed with no issues.
- `flutter test`: all 40 tests passed. Coverage includes schema v2 validation and 1→2 data preservation, transaction parsing and repository invariants, singleton draft enforcement, draft restoration/sanitation/clearing, shared month state, grouped transaction rows, inline category creation, unsaved edit dismissal, update, confirmed delete, and dismissal blocking during a delayed save.
- Offline `flutter build apk --debug`: passed. Gradle emitted the pre-existing SDK XML version compatibility warning but produced `build/app/outputs/flutter-apk/app-debug.apk` successfully.
- APK SHA-256: `e143176fd3b652856f5593e04e01239ee1cec88a47c31fcae8f62eba1b2d221a` (194,995,850 bytes).
- `apksigner verify --verbose --print-certs`: passed with one Android debug signer and APK Signature Scheme v2.
- `aapt dump badging`, `permissions`, and manifest inspection: package `site.yukiho.ledger`, version `0.1.0+1`, application label `Ledger`, `allowBackup=false`, and no `android.permission.INTERNET`.

## Environment limits

- No USB command was run. USB automation remains suspended because libusb access resets the host USB controller.
- The local Android emulator remains unavailable because hardware virtualization support is absent. Stage 3 therefore has no emulator or physical-device runtime result; automated tests and offline APK inspection are the completed Android verification available in this environment.

## Handoff

Stage 3 is complete. Stop before Stage 4 statistics until the user explicitly authorizes it.
