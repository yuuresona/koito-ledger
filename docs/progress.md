# Current progress

- **Current phase:** MVP manual Android validation — installation and cold launch passed; animation smoothness requires diagnosis.
- **Completed capabilities:** Stage 1 foundation, Stage 2 categories, Stage 3 transactions, and Stage 4 statistics. Statistics include reactive monthly income, expense, and balance totals; separate income/expense category hierarchies; same-side percentages; conditional `No subcategory` rows; integer-only CNY formatting; shared month navigation; empty and retryable error states; and immediate refresh after transaction or category changes. The Stage 1–4 MVP implementation and its automated verification baseline are complete.
- **Work explicitly in progress:** Publish the current validation record, then diagnose the user's report that animations in the installed debug build feel heavy and occasionally janky. Remaining functional smoke-test results are still pending.
- **Blockers:** No installation or launch blocker remains. A final MVP runtime verdict requires separating debug-mode overhead from any application-level frame-performance defect and completing the remaining manual interaction test.
- **Most recent verification:** On 2026-09-25, a fresh universal debug APK was built from the current source. The APK is V2-signed with the Android debug certificate, zip-aligned, targets API 36 with minimum API 24, contains ARM64, ARMv7, and x86_64 native code, has `android:allowBackup="false"`, and requests no Android `INTERNET` permission. Package metadata is `site.yukiho.ledger`, version `0.1.0` (`versionCode` 1). Its SHA-256 is `b3e318e3ac583675199564aa6dedefaf9dac59d054a6fc1c705be8fd2e82062c`. Streamed installation on the connected authorized Android device succeeded; a forced cold launch returned status `ok`, and `site.yukiho.ledger/.MainActivity` was confirmed as the top resumed activity. The user subsequently reported visibly less-fluid animations in this debug build; the cause has not yet been measured. The most recent automated baseline remains `flutter analyze` with no issues and all 67 tests passing, including the deterministic stress and migration suites.
- **Candidate next step:** Compare debug and optimized builds on the same device and collect frame timing around the affected transitions before changing implementation. After runtime acceptance, configure a private release signing key and build a release artifact under separate authorization.
- **Last updated:** 2026-09-25 17:17 CST. [Newest session note](session-notes/2026-09-25-1706-debug-apk-for-device-validation.md).
