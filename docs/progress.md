# Current progress

- **Current phase:** Post-MVP repository privacy hardening — non-destructive remediation is complete; published-history sanitization requires a separate destructive-action decision.
- **Completed capabilities:** Stage 1 foundation, Stage 2 categories, Stage 3 transactions, and Stage 4 statistics. Statistics include reactive monthly income, expense, and balance totals; separate income/expense category hierarchies; same-side percentages; conditional `No subcategory` rows; integer-only CNY formatting; shared month navigation; empty and retryable error states; and immediate refresh after transaction or category changes. The Stage 1–4 MVP implementation and its automated verification baseline are complete.
- **Work explicitly in progress:** Commit the verified hardening changes, create a recoverable local backup, and sanitize the published `main` history under the user's explicit authorization. Replace the remote branch only with a verified force-with-lease update.
- **Blockers:** Automated Android runtime verification is unavailable in the current environment. The user previously chose to skip the remaining Stage 2 physical-device test; no Stage 3 or Stage 4 physical-device test was attempted.
- **Most recent verification:** On 2026-09-25, the tracked tree and every reachable commit were audited for common credential, private-key, signing-key, embedded-authentication, local-database, and release-artifact patterns. No secret or user ledger data was found. Repository ignore rules now cover common secrets and local artifacts, public documentation no longer identifies the verification machine or test-device model, the dependency lock uses the official `pub.dev` source without version or hash changes, and future commits in this clone use a noreply address with path-scoped HTTPS credentials. `flutter analyze` reported no issues, all 67 tests passed, and `git diff --check` passed.
- **Candidate next step:** Decide whether to rewrite and force-push the small published history to remove the earlier personal commit address and machine-specific text from all historical objects. Separately, enable GitHub email privacy and command-line push blocking in account settings. Manual Android smoke testing and release packaging remain outside this hardening task.
- **Last updated:** 2026-09-25 13:17 CST. [Newest session note](session-notes/2026-09-25-1317-repository-privacy-hardening.md).
